import React from 'react'
// import testConfig from "./test.json";

// TODO: choose a json file to display
// TODO: choose a trial or go through the whole thing?
// TODO: render a new experiment in an iframe, with the config we want, while passing the values we want.

// TODO: clear the indices.

export default () => {
  return (
    <iframe title='hello'>
      <html>
        <body>
          <h1>hello world</h1>
        </body>
      </html>
    </iframe>
  )
}
