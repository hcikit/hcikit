import React from "react";
// import testConfig from "./test.json";

// TODOLATER: choose a json file to display
// TODOLATER: choose a trial or go through the whole thing?
// TODOLATER: render a new experiment in an iframe, with the config we want, while passing the values we want.

// TODOLATER: clear the indices.

export default () => {
  return (
    <iframe title="hello">
      <html>
        <body>
          <h1>hello world</h1>
        </body>
      </html>
    </iframe>
  );
};
